name: backoffice
service:
  env:
    - name: OPENAI_API_KEY
      valueFrom:
        secretKeyRef:
          key: OPENAI_API_KEY
          name: api-key-secret
  image: samirachoscalac/akka-demo:backoffice-1.0.2
  resources:
    runtime:
      mode: embedded
